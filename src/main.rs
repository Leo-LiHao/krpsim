extern crate krpsim;

/*
fn check_ressource(need: krpsim::ressource::Ressource, owned: Vec<Ressource>) -> bool  {
    let (stri, num) = need;
    let ret = owned.into_iter().any(|(ref s, x)| *s == stri && x >= num);
}
*/

fn main () {
}
